import kegAndBarrel from './keg-and-barrel'

// To add a future project: create a new file in this folder following
// keg-and-barrel.js's shape, then register it here by its `id`.
const caseStudies = {
  [kegAndBarrel.id]: kegAndBarrel,
}

export default caseStudies
